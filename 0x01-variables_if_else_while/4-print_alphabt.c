#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - entry point
 * Returns: always 0 (success)
 */
int main(void)
{
	int q, e, lowercase;

	for (lowercase = 'a'; lowercase <= 'z' lowercase++)
	{
		if (lowercase != 'e' && lowercase != 'q')
			putchar(lowercase)'
	}
	putchar("\n")
		return (0);
}
